# 前端复刻实施计划（Phase 2）

## 1. 目标说明

- 本阶段目标是基于当前后端框架（FastAPI + Celery + MongoDB）完成正式前端重构。
- 前端不再沿用 Streamlit 作为主形态，改为工程化 Web 前端（Vue3 + Vite + Element Plus）。
- UI 风格参考你提供的平台截图：借鉴整体布局与蓝色系视觉，不照搬业务内容。

## 2. 原项目功能盘点（需复刻）

原 `streamlit_webui.py + web_pages/*` 功能共 9 类：

1. 服务状态（status）
2. GPC 分析
3. NMR 分析
4. NMRServer（正向预测/反向预测/数据库搜索）
5. Raman/IR 分析
6. LC-MS（占位）
7. 数据管理
8. 问答对话
9. 验收测试

## 3. 新前端信息架构（按可落地优先）

### 3.1 P0（立即可做，依赖现有后端）

1. 工作台（Dashboard）
2. 任务提交（GPC/NMR）
3. 任务中心（列表、筛选、状态、进度）
4. 任务详情（状态时间线、结构化结果、文本报告）
5. 文件上传（对接 `/api/v1/files/upload`）

### 3.2 P1（需补后端后落地）

1. Raman/IR 分析页
2. NMRServer 三子页
3. 数据管理中心

### 3.3 P2（系统增强）

1. 问答对话页
2. 验收测试页
3. LC-MS 占位转实际流程

## 4. 布局与视觉规范

1. 左侧深色导航栏（分组菜单 + 图标）
2. 顶部浅色工具栏（环境/用户区）
3. 主区域卡片化（指标、功能入口、状态）
4. 蓝色系主题（深蓝导航 + 浅灰蓝背景 + 白卡）
5. 统一圆角、阴影、间距，支持桌面与移动端

## 5. 页面与接口映射

### 5.1 已可直接对接接口

1. `GET /api/v1/health`
2. `POST /api/v1/files/upload`
3. `POST /api/v1/tasks/gpc`
4. `POST /api/v1/tasks/nmr`
5. `GET /api/v1/tasks`
6. `GET /api/v1/tasks/{task_id}`
7. `GET /api/v1/tasks/{task_id}/result`

### 5.2 后端缺口接口（要复刻原功能必须补）

1. `POST /api/v1/tasks/ir`
2. `POST /api/v1/tasks/raman`
3. `POST /api/v1/nmrserver/forward`
4. `POST /api/v1/nmrserver/reverse`
5. `POST /api/v1/nmrserver/search`
6. 数据中心 CRUD 接口（分页/筛选/新增/删除）
7. 对话 API（会话与工具调用）
8. 验收测试 API（触发、进度、报告）

## 6. 实施里程碑

### M1（2026-04-10 ~ 2026-04-13）

1. 前端工程基线搭建（Vue3 + Vite + Element Plus + Router + Axios）
2. 全局布局壳（侧栏/顶栏/工作台）
3. 任务提交页（GPC/NMR）
4. 任务中心 + 任务详情页

交付验收：上传 -> 提交 -> 状态 -> 结果闭环可运行。

### M2（2026-04-14 ~ 2026-04-18）

1. GPC/NMR 结果可视化增强
2. 工作台统计与快捷入口完善
3. 错误态/空态/加载态统一

交付验收：可稳定演示 Phase 2 MVP。

### M3（后端缺口补齐后）

1. Raman/IR、NMRServer、数据管理中心
2. 问答对话、验收测试
3. 全量功能等价覆盖原 Streamlit 能力

## 7. 风险与应对

1. 当前后端仅完整覆盖 GPC/NMR
- 应对：先交付 P0 主链路，P1/P2 依赖后端并行推进。

2. 原功能存在本地文件与会话耦合
- 应对：统一改为 API 驱动，前端不直接依赖本地运行时逻辑。

3. 后端结果结构可能继续演进
- 应对：前端建立 DTO 适配层，降低接口变更影响。

## 8. 执行原则

1. 先完成可运行闭环，再补齐高级能力。
2. 每个页面按“可提交、可演示、可验收”推进。
3. 保持与原项目能力边界一致，逐步替换实现方式。
