# 验收样本清单（重构版）

## 1. 文档目的

- 定义重构项目各阶段的验收样本与判定标准。
- 确保重构后输出口径与源项目关键能力保持一致。
- 作为自动化回归脚本与人工验收的统一基线。

## 2. 阶段范围

- `P0（当前）`：GPC + NMR + 文件上传 + 任务中心 + 任务详情
- `P1（下一阶段）`：IR/Raman + NMRServer + 数据管理
- `P2（后续）`：问答对话 + 验收中心可视化 + LC-MS 占位完善

## 3. 样本分层规则

- `L1 基础样本`：主流程必过，要求 100% 通过。
- `L2 复杂样本`：边界/稳定性样本，要求 >= 95% 通过。
- `L3 异常样本`：必须“可解释失败”，不得出现服务崩溃。

## 4. P0 验收样本（当前执行）

## 4.1 GPC 样本

| 样本ID | 层级 | 输入类型 | 覆盖点 | 预期结果 | 当前状态 |
| --- | --- | --- | --- | --- | --- |
| GPC-L1-001 | L1 | 单个 `.arw` | 主流程（峰检测/分子量/报告） | `SUCCESS` 且有结构化结果 | 待批量复核 |
| GPC-L1-002 | L1 | `file_id` 提交 | 上传到任务全链路 | 可成功执行 | 待执行 |
| GPC-L2-001 | L2 | `detect_mode=manual` | 手动区间参数生效 | 手动区间被应用 | 待执行 |
| GPC-L2-002 | L2 | 含对比 PDF 参数 | PDF 对照路径容错 | 成功或可解释失败 | 待执行 |
| GPC-L3-001 | L3 | 不存在路径 | 路径校验 | `FAILED` + 明确错误 | 待执行 |
| GPC-L3-002 | L3 | 非法文件类型 | 类型校验 | 400 参数错误 | 待执行 |

## 4.2 NMR 样本

| 样本ID | 层级 | 输入类型 | 覆盖点 | 预期结果 | 当前状态 |
| --- | --- | --- | --- | --- | --- |
| NMR-L1-001 | L1 | Bruker 目录（1H） | 单阶段主流程 | `SUCCESS` 且结果完整 | 待批量复核 |
| NMR-L1-002 | L1 | Bruker 目录（13C） | 核种参数路径 | 参数生效，任务成功 | 待执行 |
| NMR-L1-003 | L1 | zip 上传 + `file_id` | 目录上传链路 | 解压后可分析 | 已具备能力，待验收记录 |
| NMR-L2-001 | L2 | 自定义参数组合 | 全参数透传与稳定性 | 成功或可解释失败 | 待执行 |
| NMR-L2-002 | L2 | 自定义检测区间 | `detection_range_mode=custom` | 传参生效 | 待执行 |
| NMR-L3-001 | L3 | 不存在目录 | 路径校验 | `FAILED` + 明确错误 | 待执行 |
| NMR-L3-002 | L3 | 非法参数（负阈值） | 参数校验 | 422/400 错误 | 待执行 |

## 4.3 任务中心与详情样本

| 样本ID | 层级 | 接口/页面 | 覆盖点 | 预期结果 | 当前状态 |
| --- | --- | --- | --- | --- | --- |
| TASK-L1-001 | L1 | `POST /tasks/gpc` | 创建任务 | 返回 `task_id` | 已验证 |
| TASK-L1-002 | L1 | `POST /tasks/nmr` | 创建任务 | 返回 `task_id` | 已验证 |
| TASK-L1-003 | L1 | `GET /tasks/{task_id}` | 状态流转 | `QUEUED->RUNNING->SUCCESS/FAILED` | 已验证 |
| TASK-L1-004 | L1 | `GET /tasks/{task_id}/result` | 成功/失败结果查询 | 结构化结果或错误对象 | 已验证 |
| TASK-L1-005 | L1 | `GET /tasks/{task_id}/artifacts` | 图像产物列表 | 返回产物 URL 列表 | 已验证 |
| TASK-L2-001 | L2 | `GET /tasks?page...` | 分页/筛选 | 分页与过滤正确 | 待执行 |
| TASK-L2-002 | L2 | 10+ 并发提交 | 队列稳定性 | 无丢任务 | 待执行 |
| TASK-L3-001 | L3 | 不存在 task_id | 异常查询 | 返回 404 业务错误 | 已验证 |

## 4.4 文件上传样本

| 样本ID | 层级 | 接口 | 覆盖点 | 预期结果 | 当前状态 |
| --- | --- | --- | --- | --- | --- |
| FILE-L1-001 | L1 | `POST /files/upload` | 单文件上传 | 返回 `file_id/file_name/storage_path` | 已验证 |
| FILE-L1-002 | L1 | 上传后提交任务 | 任务读取上传文件 | 任务执行成功 | 已验证 |
| FILE-L2-001 | L2 | 大文件接近上限 | 大小限制边界 | 不超过阈值可上传 | 待执行 |
| FILE-L3-001 | L3 | 空文件上传 | 空文件校验 | 400 错误 | 已验证 |
| FILE-L3-002 | L3 | 不支持类型 | 扩展名白名单 | 400 错误 | 已验证 |

## 5. P1 预备验收样本（待接口落地后执行）

## 5.1 IR/Raman

- IR-L1-001：标准 txt/csv 输入，任务成功并输出结构化结果。
- IR-L3-001：非法波数范围，返回参数错误。
- RAMAN-L1-001：标准样本输入，任务成功并输出结果图。

## 5.2 NMRServer

- NMRS-FWD-L1-001：SMILES 正向预测成功。
- NMRS-REV-L1-001：化学位移反向预测成功。
- NMRS-DB-L1-001：数据库搜索成功。
- NMRS-L3-001：下游服务超时，返回可解释错误。

## 5.3 数据管理

- DATA-L1-001：结果分页查询。
- DATA-L1-002：删除记录并同步清理索引。
- DATA-L3-001：非法 ID 查询返回 404。

## 6. 验收判定标准

- L1：100% 通过。
- L2：>= 95% 通过。
- L3：100% 返回可解释错误，且服务不崩溃。
- 一致性：核心指标与源项目偏差在预设阈值内。
- 工程性：接口字段与前端使用字段一致，无契约歧义。

## 7. 执行记录模板（每次验收后追加）

| 日期 | 执行人 | 阶段 | 样本范围 | 通过率 | 失败样本 | 问题单/备注 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-04-11 | 待填写 | P0 | 待填写 | 待填写 | 待填写 | 待填写 |

## 8. 当前待补信息

1. 每个样本的真实输入路径（统一整理到测试配置中）。
2. GPC 对比 PDF 与校准文件映射关系。
3. NMR 内标峰判定阈值（溶剂峰/TMS）明确化。
4. 错误码与错误消息标准文案统一。

## 9. 版本记录

- v0.1（2026-04-10）：P0 首版。
- v0.2（2026-04-11）：按当前实现重写，增加“当前状态”和“执行记录模板”。
