# 验收样本清单

## 1. 文档目的

- 定义重构 Phase 0 ~ Phase 1 的验收样本范围
- 保证重构后系统在核心能力上与源项目输出口径一致
- 为后续自动化回归与验收测试提供固定基线

## 2. 验收范围说明

- 当前阶段仅覆盖 P0 能力：`GPC + NMR + 任务中心 + 文件上传 + 任务查询`
- IR/Raman 能力延后到 P1，不在本轮验收样本范围内
- NMR 采用单阶段任务策略：首次调用解析 API 即给全参数，内标峰默认由溶剂峰或 TMS 峰自动确定

## 3. 样本分层策略

- `L1 基础样本`：用于主流程通路验证（必过）
- `L2 复杂样本`：用于边界和稳定性验证（应过）
- `L3 异常样本`：用于错误处理和健壮性验证（应正确失败）

## 4. GPC 验收样本清单（P0）

| 样本ID | 层级 | 输入类型 | 目标覆盖点 | 预期结果 |
| --- | --- | --- | --- | --- |
| GPC-L1-001 | L1 | 单个 `.arw` 文件 | 标准主流程（ROI/峰检测/分子量计算/报告生成） | 任务成功，返回结构化结果与报告 |
| GPC-L1-002 | L1 | 目录（含多个 `.arw`） | 批量扫描与逐样本结果生成 | 任务成功，样本数与输入一致 |
| GPC-L1-003 | L1 | 显式三色曲线 + 校准文件 | 覆盖显式传参路径 | 任务成功，ROI 正常识别 |
| GPC-L2-001 | L2 | 手动积分区间参数 | 覆盖 `manual` 检测模式 | 任务成功，积分区间按传参生效 |
| GPC-L2-002 | L2 | 对比 PDF 报告文件 | 覆盖 PDF 提取与误差分析 | 任务成功，输出误差指标 |
| GPC-L3-001 | L3 | 不存在路径 | 输入校验错误路径 | 任务失败，返回明确错误信息 |
| GPC-L3-002 | L3 | 非 `.arw` 文件 | 文件类型校验 | 任务失败，返回格式错误信息 |

## 5. NMR 验收样本清单（P0）

| 样本ID | 层级 | 输入类型 | 目标覆盖点 | 预期结果 |
| --- | --- | --- | --- | --- |
| NMR-L1-001 | L1 | Bruker 标准目录（1H） | 单阶段主流程（峰检测/积分/归一化/报告） | 任务成功，输出完整结果 |
| NMR-L1-002 | L1 | Bruker 标准目录（13C） | 核类型差异参数路径 | 任务成功，参数按核类型生效 |
| NMR-L1-003 | L1 | 包含溶剂峰样本 | 默认内标峰自动识别（溶剂峰） | 任务成功，内标峰自动选择有效 |
| NMR-L1-004 | L1 | 包含 TMS 峰样本 | 默认内标峰自动识别（TMS） | 任务成功，内标峰自动选择有效 |
| NMR-L2-001 | L2 | 高噪声样本 | 平滑与阈值参数鲁棒性 | 任务成功或可解释失败 |
| NMR-L2-002 | L2 | 自定义参数组合 | 覆盖首次调用全参数传入 | 任务成功，参数回显一致 |
| NMR-L3-001 | L3 | 不存在目录 | 输入路径校验 | 任务失败，错误信息明确 |
| NMR-L3-002 | L3 | 非法参数（如阈值负值） | 参数校验 | 任务失败，返回参数错误 |

## 6. 任务中心与接口验收样本（P0）

| 样本ID | 层级 | 接口 | 目标覆盖点 | 预期结果 |
| --- | --- | --- | --- | --- |
| TASK-L1-001 | L1 | `POST /api/v1/tasks/gpc` | GPC 任务提交 | 返回 `task_id`，状态为 `PENDING/QUEUED` |
| TASK-L1-002 | L1 | `POST /api/v1/tasks/nmr` | NMR 任务提交 | 返回 `task_id`，状态为 `PENDING/QUEUED` |
| TASK-L1-003 | L1 | `GET /api/v1/tasks/{task_id}` | 任务状态查询 | 状态可从 `QUEUED` 过渡到 `RUNNING/SUCCESS` |
| TASK-L1-004 | L1 | `GET /api/v1/tasks/{task_id}/result` | 结果查询 | 成功任务可获取结构化结果与报告 |
| TASK-L2-001 | L2 | 并发提交 10+ 任务 | 队列稳定性 | 全部任务可追踪，无丢任务 |
| TASK-L3-001 | L3 | 查询不存在任务ID | 异常查询处理 | 返回 404 或业务错误码 |

## 7. 文件上传验收样本（P0）

| 样本ID | 层级 | 接口 | 目标覆盖点 | 预期结果 |
| --- | --- | --- | --- | --- |
| FILE-L1-001 | L1 | `POST /api/v1/files/upload` | 上传单文件 | 返回文件ID与存储路径 |
| FILE-L1-002 | L1 | 上传后提交任务 | 文件到任务链路打通 | 任务可读取上传文件并执行 |
| FILE-L3-001 | L3 | 上传空文件 | 输入校验 | 返回参数错误 |
| FILE-L3-002 | L3 | 上传不支持类型 | 类型校验 | 返回类型错误 |

## 8. 验收判定标准（P0）

- 主流程通过率：L1 样本 100% 通过
- 稳定性：L2 样本通过率 >= 95%
- 异常处理：L3 样本需返回可解释错误，不得出现服务崩溃
- 结果一致性：核心指标与源项目对照偏差在预设阈值内

## 9. 待补充信息（执行前必须落地）

1. 每个样本的真实文件路径与归档目录
2. GPC 对照 PDF 与校准文件映射关系
3. NMR 溶剂峰/TMS 峰判定规则的参数阈值
4. 各类错误的统一错误码规范

## 10. 版本记录

- v0.1（2026-04-10）：按已确认 P0 范围生成首版验收样本清单
