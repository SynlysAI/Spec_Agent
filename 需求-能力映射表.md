# 需求-能力映射表

## 1. 文档目的

- 用于在重构过程中建立“需求 -> 现有能力 -> 目标架构落点”的统一映射
- 约束重构范围，避免遗漏核心能力或引入无计划变更
- 作为后续 API 设计、任务拆解、验收测试的基线文档

## 2. 重构范围基线

- 源项目：`E:\github_project\Spec_Agent`
- 重构项目：`E:\xx_project\Spec_Agent`
- 当前重构目标：将 `streamlit` Demo 架构升级为可正式上线的工程化系统（`FastAPI + Vue3 + Celery + RabbitMQ + MongoDB`）

## 3. 能力映射总表

| 编号 | 需求能力 | 源项目现状（模块） | 重构目标落点（模块） | 优先级 | 迁移风险/备注 |
| --- | --- | --- | --- | --- | --- |
| R01 | GPC 谱图智能分析 | `agents/langraph_gpc_agent.py`、`analysis/gpc/*`、`services/gpc_service.py`、`web_pages/gpc_interface.py` | `backend/domain/gpc/*`、`backend/workers/tasks_gpc.py`、`backend/api/v1/tasks_gpc.py`、`frontend/pages/gpc/*` | P0 | 长耗时任务需异步化；输入文件与结果图需按任务隔离 |
| R02 | NMR 谱图智能分析 | `agents/langraph_nmr_agent.py`、`analysis/nmr/*`、`services/nmr_service.py`、`web_pages/nmr_interface.py` | `backend/domain/nmr/*`、`backend/workers/tasks_nmr.py`、`backend/api/v1/tasks_nmr.py`、`frontend/pages/nmr/*` | P0 | 改为单阶段任务：首次调用即提交完整参数；内标峰默认按溶剂峰或 TMS 峰自动确定 |
| R03 | IR/Raman 智能分析 | `agents/ir_raman_agent.py`、`analysis/raman/*`、`web_pages/raman_interface.py` | `backend/domain/ir_raman/*`、`backend/workers/tasks_ir_raman.py`、`backend/api/v1/tasks_ir_raman.py`、`frontend/pages/ir_raman/*` | P1 | 模型加载重，需缓存模型实例并控制并发 |
| R04 | NMRServer 正反向预测/数据库搜索 | `services/nmr_server_service.py`、`web_pages/nmr_server_interface.py` | `backend/integrations/nmr_server_client.py`、`backend/api/v1/nmr_server.py`、`frontend/pages/nmr_server/*` | P1 | 外部服务稳定性与超时重试策略需标准化 |
| R05 | 智能问答对话 | `web_pages/dialogue.py`、`services/dialogue_service.py`、`llm_server/model.py` | `backend/domain/dialogue/*`、`backend/api/v1/chat.py`、`frontend/pages/chat/*` | P1 | 会话历史与提示词增强需入库（MongoDB） |
| R06 | 服务状态监控面板 | `web_pages/status_interface.py` | `backend/api/v1/health.py`、`backend/api/v1/metrics.py`、`frontend/pages/ops_dashboard/*` | P1 | 需增加真实运行指标，替代前端静态状态展示 |
| R07 | 数据管理（谱图/结果） | `web_pages/database_interface.py`、`services/database_service.py` | `backend/api/v1/files.py`、`backend/api/v1/results.py`、`frontend/pages/data_center/*` | P1 | 文件元数据与结果索引需统一模型 |
| R08 | 验收测试能力 | `tests/acceptance/*`、`web_pages/acceptance_interface.py` | `backend/scripts/acceptance_runner.py`、`backend/api/v1/acceptance.py`、`frontend/pages/acceptance/*` | P2 | 建议保留离线批跑优先，前端展示作为增强 |
| R09 | CLI 能力 | `spec_cli/cli.py`、`spec_collector/cli.py` | `backend/cli/*`（保留并适配新配置） | P2 | 作为运维入口保留，不阻塞 Web 上线 |
| R10 | 谱图采集子系统 | `spec_collector/*` | `collector_service/*`（可独立部署） | P2 | 与主分析服务解耦，减少发布耦合风险 |

## 4. 横向能力映射（非业务功能）

| 编号 | 横向能力需求 | 源项目现状 | 重构目标落点 | 优先级 | 备注 |
| --- | --- | --- | --- | --- | --- |
| X01 | 统一任务中心 | 主要由 Streamlit 页面触发，无统一任务模型 | `backend/application/task_service.py` + MongoDB `tasks` 集合 | P0 | 所有长任务统一任务ID和状态机 |
| X02 | 异步执行与队列 | 以本地同步执行为主 | `Celery + RabbitMQ` | P0 | 提交即返回，Worker 后台执行 |
| X03 | 结果持久化 | 输出目录分散 | MongoDB `analysis_results` + 文件目录规范 | P0 | 文件和元数据分离存储 |
| X04 | 文件上传与管理 | 主要依赖本地路径输入 | `POST /files/upload` + `files` 集合 | P0 | 统一 hash、归属、生命周期 |
| X05 | 鉴权与权限 | Demo 阶段弱化 | `backend/auth/*` + 前端登录态 | P1 | 二期可接企业 SSO |
| X06 | 可观测性 | 页面状态展示为主 | 结构化日志 + 指标 + 追踪ID | P1 | 先日志和任务指标，再接监控平台 |
| X07 | 配置分环境 | 本地路径和环境变量混用 | `settings/dev/test/prod` + `.env` 分层 | P0 | 消除硬编码路径依赖 |
| X08 | 部署与回滚 | Demo 启动方式为主 | `docker-compose` 标准部署 | P1 | 发布、回滚、扩容流程化 |

## 5. Phase 0 拆解（当前执行项）

1. 完成《需求-能力映射表》
2. 产出《验收样本清单》
3. 产出《API 草案（v1）》

## 6. 当前待你确认的关键决策

1. P0 范围仅包含：`GPC + NMR + 任务中心 + 文件上传 + 任务查询`（已确认）
2. IR/Raman 延后到 P1 全量接入（已确认）
3. NMR 不采用两段式任务；内标峰默认由溶剂峰或 TMS 峰确定，首次调用解析 API 时一次性给出全部参数（已确认）

## 7. 版本记录

- v0.1（2026-04-10）：初版映射，基于源项目模块结构与重构目标架构制定
- v0.2（2026-04-10）：已固化关键决策：P0 仅 GPC/NMR 主链路，IR/Raman 延后至 P1，NMR 改为单阶段默认内标峰方案
