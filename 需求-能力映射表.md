# 需求-能力映射表（重构版）

## 1. 文档目的

- 建立“源项目能力 -> 重构目标 -> 当前实现 -> 验收标准”的统一映射。
- 作为后续迭代排期、联调验收、上线评审的基准文档。
- 明确哪些能力已经完成、哪些能力仍在迁移中，避免遗漏。

## 2. 范围与基线

- 源项目：`E:\github_project\Spec_Agent`
- 重构项目：`E:\xx_project\Spec_Agent`
- 源入口基线：`streamlit_webui.py`（共 9 类能力）
- 当前日期基线：`2026-04-11`

## 3. 业务能力映射总表

| 编号 | 能力项 | 源项目落点 | 重构目标落点 | 优先级 | 当前状态 | 完成度 | 验收口径 | 备注/风险 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R01 | GPC 谱图智能分析 | `web_pages/gpc_interface.py`、`agents/langraph_gpc_agent.py`、`services/gpc_service.py` | 后端 `POST /api/v1/tasks/gpc` + 前端 `TaskSubmitGpcView/TaskDetailView` | P0 | 已打通 | 90% | 提交、执行、结果查询、图像产物可展示 | 当前仍依赖源项目运行时导入 |
| R02 | NMR 谱图智能分析（单阶段） | `web_pages/nmr_interface.py`、`agents/langraph_nmr_agent.py`、`services/nmr_service.py` | 后端 `POST /api/v1/tasks/nmr` + 前端 `TaskSubmitNmrView/TaskDetailView` | P0 | 已打通 | 90% | 单阶段提交成功，自动内标策略可执行 | 目录上传为 zip 打包路径，需补更多异常样本 |
| R03 | IR/Raman 分析 | `web_pages/raman_interface.py`、`agents/ir_raman_agent.py` | 后端 `POST /api/v1/tasks/ir|raman` + 前端页面 | P1 | 未开始（仅前端占位） | 10% | 支持提交、执行、结果可视化 | 模型加载与推理资源管理风险高 |
| R04 | NMRServer（正向/反向/数据库搜索） | `web_pages/nmr_server_interface.py`、`services/nmr_server_service.py` | 后端 `api/v1/nmrserver/*` + 前端三子页 | P1 | 未开始 | 0% | 三接口联调通过，异常超时可控 | 外部服务可用性与超时重试需标准化 |
| R05 | 智能问答对话 | `web_pages/dialogue.py`、`services/dialogue_service.py` | 后端 `api/v1/chat/*` + 前端对话页 | P1 | 未开始 | 0% | 支持会话历史、报告增强问答 | LLM 成本与会话持久化策略待定 |
| R06 | 服务状态监控面板 | `web_pages/status_interface.py` | 后端 `health/metrics` + 前端监控页 | P1 | 部分完成 | 30% | 非静态状态，包含真实依赖探测 | `health.worker` 当前为固定值 |
| R07 | 数据管理中心 | `web_pages/database_interface.py`、`services/database_service.py` | 后端 `files/results` CRUD + 前端数据中心 | P1 | 未开始 | 0% | 支持分页、筛选、删除、下载 | 数据模型统一与索引设计待补 |
| R08 | 验收测试中心 | `web_pages/acceptance_interface.py`、`tests/acceptance/*` | 后端验收触发 API + 前端验收页 + 脚本 | P2 | 部分完成（仅脚本雏形） | 25% | 批量执行、指标汇总、报告归档 | 优先建议先脚本化再可视化 |
| R09 | LC-MS（占位） | `streamlit_webui.py` 占位入口 | 新前后端占位与后续计划 | P2 | 未开始 | 0% | 页面/接口占位明确，不影响主链路 | 暂不阻塞上线 |

## 4. 横向能力映射（工程化能力）

| 编号 | 横向能力 | 当前状态 | 完成度 | 目标状态 |
| --- | --- | --- | --- | --- |
| X01 | 统一任务中心（状态机 + 列表筛选） | 已实现 `tasks` 集合与查询接口 | 85% | 增加取消、重试、幂等能力 |
| X02 | 异步执行（Celery + RabbitMQ） | 已运行 | 85% | 增加超时、重试策略与告警 |
| X03 | 文件治理（上传/存储/索引） | 已实现上传与元数据入库 | 75% | 生命周期、清理策略、下载鉴权 |
| X04 | 结果持久化与产物托管 | 已实现结果落库和静态产物访问 | 80% | 版本化结果模型与长期存储策略 |
| X05 | 配置分层（dev/test/prod） | 仅基础 `.env` 方式 | 40% | 完整环境分层与密钥治理 |
| X06 | 可观测性（日志/指标/追踪） | 基础日志，缺指标与追踪 | 25% | 结构化日志 + 指标 + trace id |
| X07 | 鉴权与权限 | 未实现 | 0% | API 鉴权与前端登录态 |
| X08 | 独立部署能力（脱离源项目） | 未完成 | 20% | 不依赖 `SOURCE_SPEC_AGENT_ROOT` |

## 5. 已落地能力证据（当前仓）

- 后端接口：`backend/openapi.json`
- 异步任务执行：`backend/app/worker/tasks.py`
- 任务服务：`backend/app/services/task_service.py`
- 文件服务：`backend/app/services/file_service.py`
- 前端任务主链路：`frontend/src/views/TaskSubmitGpcView.vue`、`frontend/src/views/TaskSubmitNmrView.vue`、`frontend/src/views/TaskCenterView.vue`、`frontend/src/views/TaskDetailView.vue`
- 回归脚本：`backend/scripts/run_regression.py`

## 6. 当前关键结论

1. P0 主链路（GPC/NMR）已具备演示条件。
2. 功能等价覆盖尚未完成，当前覆盖源项目 9 类能力中的核心 2 类。
3. 上线阻塞项是“运行时依赖源项目路径”，必须在 P1/P2 期间完成内聚迁移。

## 7. 后续更新规则

- 每完成一个能力点，必须同步更新“当前状态/完成度/验收口径”。
- 每周至少更新一次本表，并在 `重构验收进度表.md` 记录时间戳。
- 若验收口径变更，需同时更新 `验收样本清单.md`。

## 8. 版本记录

- v0.1（2026-04-10）：初版映射。
- v0.2（2026-04-10）：固化 P0 范围与 NMR 单阶段策略。
- v0.3（2026-04-11）：按当前代码现状重写，增加状态/完成度/验收口径与证据链接。
